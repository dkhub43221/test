local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Streetz War Quick Buy",
    LoadingTitle = "Quick Buy",
    LoadingSubtitle = "By You",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "QuickBuyConfig",
        FileName = "Config"
    },
    Discord = { Enabled = false }
})

-- ===== Variables =====
local selectedGun = nil

-- ===== Functions =====
local function CannotAffordNotification(text)
    Rayfield:Notify({
        Title = "Quick Buy",
        Content = text,
        Duration = 3,
        Image = 4483362458,
        Actions = {}
    })
end

local function getGunList()
    local gunFolder = workspace:FindFirstChild("GUNS")
    if not gunFolder then return {} end
    local guns = {}
    for _, gun in pairs(gunFolder:GetChildren()) do
        local price = gun:FindFirstChild("Price") and gun.Price.Value or 0
        table.insert(guns, gun.Name .. " ($" .. price .. ")")
    end
    return guns
end

local function extractGunName(option)
    return option:match("^(.-) %(%$.-%)$") or option
end

local function purchaseGunPrompt(gunName)
    local gunObj = workspace:FindFirstChild("GUNS") and workspace.GUNS:FindFirstChild(gunName)
    if not gunObj then return false end

    local price = gunObj:FindFirstChild("Price") and gunObj.Price.Value or 0
    local playerMoney = game:GetService("Players").LocalPlayer.stored.Money.Value

    if playerMoney < price then
        CannotAffordNotification("❌ You cannot afford this weapon ($" .. price .. ")")
        return false
    end

    local prompt = gunObj:FindFirstChildOfClass("ProximityPrompt")
    if not prompt then return false end

    local player = game:GetService("Players").LocalPlayer
    local character = player.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return false end

    local oldCFrame = character.HumanoidRootPart.CFrame
    character.HumanoidRootPart.CFrame = gunObj.PrimaryPart and gunObj.PrimaryPart.CFrame or gunObj.CFrame

    local success, _ = pcall(function()
        prompt:InputHoldBegin()
        task.wait(0.1)
        prompt:InputHoldEnd()
    end)

    character.HumanoidRootPart.CFrame = oldCFrame
    return success or false
end

local function autoPurchaseGun()
    if not selectedGun then
        CannotAffordNotification("❌ Please select a gun first")
        return
    end
    purchaseGunPrompt(extractGunName(selectedGun))
end

-- ===== UI =====
local QuickBuyTab = Window:CreateTab("Quick Buy", 4483362458)

-- Dropdown for selecting gun
local GunDropdown = QuickBuyTab:CreateDropdown({
    Name = "Select Gun",
    Options = getGunList(),
    CurrentOption = "",
    Flag = "GunDropdown",
    Callback = function(option)
        if not option then return end
        selectedGun = option
    end
})

-- Purchase button
QuickBuyTab:CreateButton({
    Name = "🚀 Purchase Selected Gun",
    Callback = function()
        autoPurchaseGun()
    end
})
